/*
 * argv.js
 */

function main(argv)
{
	console.print("argv.length = " + argv.length + "\n") ;
	for(i=0 ; i<argv.length ; i++){
	    console.print("argv[" + i + "] = " + argv[i] + "\n") ;
	}
	return 0 ;
}

